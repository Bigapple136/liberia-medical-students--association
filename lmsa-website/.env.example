VITE_API_URL=http://localhost:5000/api
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_APP_NAME=LMSA
VITE_APP_URL=http://localhost:5173
# Used by PageMeta.jsx for canonical/OG/Twitter URLs. Falls back to
# https://lmsa.org.lr if unset -- set this to whatever domain is
# actually live in production (e.g. the Cloudflare Workers URL, or a
# custom domain once one is configured) or SEO metadata will point to
# the wrong place.
VITE_SITE_URL=http://localhost:5173